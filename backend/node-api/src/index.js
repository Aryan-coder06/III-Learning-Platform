/**
 * Stage 1 backend placeholder.
 * This file is intentionally minimal until the API implementation phase begins.
 */

const { env } = require("./config/env");

console.log(
  `StudySync Node API scaffold ready. Future server boot will run on port ${env.port}.`,
);
